// -*- mode: glsl -*-
#version 330 core

layout(location = 0) in vec3 vertexPosition_modelspace;
layout(loaction = 1) in vec2 vertexUV;

void main() {
	gl_Position.xyz = vertexPosition_modelspace;
	gl_Position.w = 1.0;
	UV = vertexUV;
}
